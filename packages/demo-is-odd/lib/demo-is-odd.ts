export function isOdd(num: number): boolean {
  console.log("isOdd called with:", num);
  console.log("isOdd called with222:", num);
  console.log("isOdd called with333:", num);
  return num % 2 !== 0;
}